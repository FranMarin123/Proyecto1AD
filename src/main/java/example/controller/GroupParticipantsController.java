package example.controller;

import example.controller.Controller;
import javafx.fxml.Initializable;

import java.io.IOException;
import java.net.URL;
import java.util.ResourceBundle;

/**
 * Controller for managing the group participants view.
 * Provides lifecycle methods for initializing and managing
 * actions when opening and closing the controller.
 */
public class GroupParticipantsController extends Controller implements Initializable {
    @Override
    public void onOpen(Object input) throws IOException {

    }

    @Override
    public void onClose(Object output) {

    }

    @Override
    public void initialize(URL url, ResourceBundle resourceBundle) {

    }
}
